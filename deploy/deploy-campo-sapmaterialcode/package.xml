<?xml version="1.0" encoding="UTF-8"?>
<Package xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Chave de integracao SAP<->Salesforce do catalogo (MATNR):
         unico + External ID -> upsert idempotente pela Metadata/REST API
         e garantia anti-duplicado do banco. Criado na US-021; este pacote
         leva SO o campo para outros ambientes (ex.: INT da replica Mule) -->
    <types>
        <members>Product2.SapMaterialCode__c</members>
        <name>CustomField</name>
    </types>
    <version>63.0</version>
</Package>
