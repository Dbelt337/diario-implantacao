<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>ValidaCamposObrigatorios</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
				OR(
				    ISBLANK(Street),
				    ISBLANK(City),
				    ISBLANK(State),
				    ISBLANK(PostalCode),
				    ISBLANK(Country)
				),
				ISCHANGED(IsConverted),
				IsConverted
)</errorConditionFormula>
    <errorMessage>Atenção: é necessário preencher todos os campos da seção &apos;Informações de endereço&apos; antes de converter um lead</errorMessage>
</ValidationRule>
